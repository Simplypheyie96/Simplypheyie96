name: Update Spotify Now Playing

on:
  workflow_dispatch:   # manual only — real-time handled by Vercel endpoint

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - run: echo "Now Playing is handled by the live Vercel endpoint."
